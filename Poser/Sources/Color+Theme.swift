import SwiftUI

extension Color {
    /// Soft sky-blue brand accent (#AEE9F6). Used sparingly — on the mode chips
    /// and interactive controls — so it reads as an intentional accent, not decoration.
    static let poserAccent = Color(red: 174 / 255, green: 233 / 255, blue: 246 / 255)
    static let buttonAccent = Color(red: 130 / 255, green: 200 / 255, blue: 210 / 255)
}
